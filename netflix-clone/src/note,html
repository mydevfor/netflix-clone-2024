
    to find icons
npm install @mui/icons-material then search icons and copy import link  and put  vareble name on specific area 
https://codeium.com/

Enter the movie database
Setting > api > Our primary documentation is located at developer.themoviedb.org. >
Api reference  > geners > movie list > try it > {
  "genres": [
    {
      "id": 28,
      "name": "Action"
    },
    {
      "id": 12,
      "name": "Adventure"
    },
    {
      "id": 16,
      "name": "Animation"
    },
}]
How to add git hub to vsc
Open terminal
 Git clone 
echo "# netflix-clone-2024" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/birhanu-tech/netflix-clone-2024.git 
 Git add.
git commit  -m "first commmrt"
git push -u origin main
Search Mui icons > search material icons > copy npi link > paste  and install on terminal
Ø	npm install @mui/icons-material @mui/material @emotion/styled @emotion/react
Ø	Then  > search any type of icone > click open > copy url > past(import) to your project
Ø	import SearchIcon from '@mui/icons-material/Search';
How to commit 
Header and footer 

To all
{SELECT * FROM Products 
JOIN ProductDescription 
JOIN ProductPrice ON Products.product_id = ProductDescription.product_id AND Products.product_id = ProductPrice.product_id  }

{SELECT * FROM Products 
JOIN ProductDescription 
JOIN ProductPrice ON Products.product_id = ProductDescription.product_id AND Products.product_id = ProductPrice.product_id   WHERE products.product_name = 'iphone se'}
    
–	Npm i movie-trailer       then  import
–	Nom i react-youtube        then  import
–	Npm i axios            then  import
Npm js to search for npm (module) packege  exa- movie-trailer

Step To deploy 
Go to packege.json > write on top -
 "homepage": "http://birhanu-tech.github.io/netflix-clone-2024",
Then go to secret > write this two above build
"predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
 Then go to push
-Ls
–	 npm i gh-pages --save-dev
-Git add .
                   – git commit –m  deploy
                  – git push
               – npm run deploy
If succesfilly deploy will see Published
Then go to git hub > setting > pages > click  visit sitr links


Optional chaining: (?.)



